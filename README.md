# Second Brain

A Streamlit app for transcribing uploaded audio/video and chatting with the resulting transcript library.

## Configuration

Set `GROQ_API_KEY` and `GEMINI_API_KEY` in Streamlit Community Cloud secrets. Do not commit `.env` files or API keys.
